import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.things-labs.pwa',
  appName: 'WaterLevel',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
